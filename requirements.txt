tushare>=1.4.0
python-dotenv>=1.0.0
pandas>=2.0.0
requests>=2.28.0
pyyaml>=6.0.0
httpx>=0.24.0

# Web API（api/ 下一直在用，此前漏声明：api/main.py 用 fastapi、
# api/config.py 用 pydantic-settings、pyproject 的 zt-web entry point 用 uvicorn）
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic-settings>=2.0.0

# 高阶行情数据层（modules/advanced_data.py）：东财/同花顺公开接口
akshare>=1.18.0
