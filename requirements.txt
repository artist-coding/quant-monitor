tushare>=1.4.0
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.26.0
requests>=2.28.0
httpx>=0.24.0

# API 服务（api/ 全线依赖，此前漏声明：照本文件装完仍然 ImportError）。
# api/main.py 用 fastapi、api/config.py 用 pydantic-settings、
# pyproject 的 zt-web entry point 用 uvicorn。
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# 高阶行情数据层（modules/advanced_data.py）：东财/同花顺公开接口
akshare>=1.18.0
